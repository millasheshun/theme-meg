import 'package:flutter/material.dart';

class TheMegApp extends StatelessWidget {
  const TheMegApp({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor:Colors.black87,
      appBar: AppBar(

      ),
    );
  }
}